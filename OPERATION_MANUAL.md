# 伏羲平台操作总览

> 面向第一次使用伏羲平台的产品、设计和研发同学。普通使用以“完成任务”为主，不需要先理解 MCP 工具名、内部 ID 或底层协议。

## 伏羲是做什么的

伏羲负责管理 AI 原型的创建结果、版本、项目协作、预览和分享。AI 对话仍发生在你选择的 AI 工具中，伏羲不提供独立的 AI 聊天窗口。

第一次使用时，只需要记住这条路径：

```text
接入 AI → 创建原型 → 回伏羲检查结果 → 需要时修改、协作或分享
```

## 主要入口

| 入口 | 什么时候用 |
|---|---|
| 原型列表 | 创建原型、查找已有原型、进入原型详情 |
| 原型详情 | 看预览、版本、设计说明，发起独立原型修改或分享 |
| 项目 | 管理项目菜单和成员，发起项目内修改任务并审核修订 |
| 接入平台 MCP | 第一次把伏羲接入 WorkBuddy、Cursor 等 AI 工具 |
| 帮助 | 查找操作手册和故障处理 |
| 系统管理 | 管理员维护平台配置、公告和帮助内容 |

## 第一次接入 AI

1. 登录伏羲，点击「接入平台 MCP」；
2. 选择当前使用的 AI Host；
3. 把平台生成的完整提示词交给 AI；
4. 按提示重新加载客户端或打开新会话；
5. 让 AI 确认伏羲已经可以正常使用。

当前推荐 WorkBuddy，Cursor 为兼容 Host。Codex 当前不提供确定性自动安装。正常接入不要求用户或 AI 自己扫描目录、拼装安装命令。

如果 WorkBuddy 提示需要信任自定义连接器，请在「连接器 → 自定义连接器」中信任 `fuxi-platform`。

## 创建原型

从「原型列表 → 让 AI 创建原型」开始。

你主要负责说清楚：

- 谁来使用；
- 用户要完成什么；
- 必须包含哪些页面；
- 关键操作是什么；
- 哪些内容这次不做；
- 怎样算完成。

如果有需求文档，提供完整本地路径。平台会生成完整任务提示词，把它交给已经接入伏羲的 AI 即可。

AI 报告完成后，以伏羲里的真实结果为准：目标原型存在、版本正确、预览能打开、关键交互符合需求。

## 修改原型

先判断原型是否已经进入项目。

### 修改独立原型

从原型详情点击「让 AI 修改」，写清：

- 要改什么；
- 必须保留什么；
- 修改后怎样验收。

把平台生成的提示词交给 AI。独立原型修改完成后会形成新的正式版本。

### 修改项目内原型

进入项目目标节点点击「让 AI 修改」。项目里的修改不会直接覆盖正式版，而是进入审核流程：

```text
发起任务 → AI 提交待审修订 → 负责人退回或采用 → 采用后形成新的正式版本
```

当前 Task v2 不要求先签出。AI 提交修订也不代表正式版本已经更新，只有负责人采用后才会更新。

## 项目协作

项目 Owner/Admin 负责：

1. 创建项目并维护菜单；
2. 绑定已有原型；
3. 添加项目成员并设置角色；
4. 发起项目修改任务并指定负责人；
5. 审核待审修订，决定退回或采用。

任务页会区分：

- **当前正式版**：项目正在使用的结果；
- **待审修订**：AI 已提交、尚未采用的修改；
- **历史尝试**：之前被替换或退回的提交。

同一个任务任一时刻只保留一个当前待审修订。

## 预览、正式版本和分享

- **预览**：检查页面是否能打开、交互是否符合预期；
- **正式版本**：当前正式采用的版本；
- **待审修订**：等待负责人审核的项目修改；
- **分享**：给评审者一个受控预览入口，不授予平台管理权限。

分享时请发送平台生成的**完整分享链接**。如果对方打不开，让对方从完整链接重新打开，并在同一个正常浏览器中完成跳转；不要只复制跳转后的预览地址。

## 怎样向 AI 描述需求

不需要在提示词里手写工具名、任务 ID 或候选 ID。优先从伏羲页面发起任务，让平台补齐执行上下文。

你真正需要补充的是：

```text
目标用户：
核心任务：
要做：
必须保留：
不做：
验收结果：
参考文件：
```

避免只说“优化一下”“做得高级一点”“你看着办”。把主观要求改成具体场景、约束和可观察结果，可以明显减少返工。

## 什么时候需要看进阶设置

如果你只是做普通原型，可以直接使用平台默认值，不需要先理解 prototype spec、runtime profile 或输出模式。

只有在这些场景下再看进阶手册：

- 必须使用指定组件规范；
- 要证明某套组件被真实使用；
- 修改已有原型并保持原技术栈；
- AI 无法判断原有实现，需要人工确认。

## 常见问题

### AI 已经说“上传成功”，为什么还不能算完成？

上传只是中间状态。以伏羲里的目标原型、版本和真实预览为准。

### 项目修订已经提交，为什么正式版没变？

这是正常的。负责人采用后才会成为正式版本。

### 项目任务需要先签出吗？

不需要。签出/签入只保留给兼容或存量场景，不是当前 Task v2 的前置步骤。

### 为什么复制跳转后的预览地址会打不开？

请分享平台生成的完整分享链接，不要只复制跳转后的地址。

### 我没有权限怎么办？

联系项目负责人或平台管理员申请当前动作所需权限。不要借用账号、token，也不要用删除或回滚绕过权限。

## 出错时先做什么

优先保留：

- 当前原型和版本；
- 项目任务；
- AI 返回的完整错误；
- 你本来要完成的目标。

然后根据错误重新读取平台当前状态再处理。不要因为一次失败就连续重试、删除正式版本、强制覆盖或回滚。

## 帮助中心怎么选文章

- 第一次使用：看《伏羲平台快速入门》；
- 接入失败：看《接入平台 MCP》；
- 创建原型：看《让 AI 创建原型》；
- 修改原型：看《让 AI 修改原型》；
- 项目协作：看《项目协作：Task v2 与候选审核》；
- 交付和分享：看《构建、校验、发布与分享》；
- 组件规范：看《规范、运行时与输出模式》；
- 不知道怎么描述需求：看《如何向 AI 描述原型需求》；
- 遇到异常：看《常见问题》或《故障排查与安全边界》。

普通使用先解决当前任务；只有需要排障或控制实现细节时，再进入技术层。