# 伏羲平台项目化协作操作手册

> 以「天宫平台」项目为例，演示两位产品经理（陈产品、叶产品）如何在同一个项目下按模块分工协作。

---

## 一、角色与权限说明

| 角色 | 说明 | 可执行操作 |
|---|---|---|
| **创建者 / Owner** | 项目的创建人 | 配置菜单、绑定原型、管理成员、创建快照、删除项目 |
| **管理员 / Admin** | 平台管理员 | 与 Owner 权限相同 |
| **编辑者 / Editor** | 被邀请参与协作的产品/设计人员 | 签出自己负责的模块、上传更新、签入释放 |
| **查看者 / Reviewer** | 只能查看和评论的人员 | 查看项目门户、预览原型、发表评论 |

在本手册中，假设：
- **张管理员** 是平台管理员，负责创建项目和邀请成员。
- **陈产品** 负责「实体建模」模块。
- **叶产品** 负责「方法建模」模块。

---

## 二、前置准备：张管理员创建项目并邀请成员

### 2.1 创建「天宫平台」项目

1. 登录伏羲平台，点击顶部导航 **「项目」**。
2. 点击右上角 **「创建项目」**。
3. 填写项目信息：
   - **项目名称**：天宫平台
   - **项目描述**：天宫低代码开发平台原型管理
4. 配置菜单结构（支持一级菜单 + 二级菜单）：

```
天宫平台
├── 实体建模
│   ├── 实体列表
│   ├── 实体详情
│   └── 关系图谱
├── 方法建模
│   ├── 方法编辑器
│   └── 方法执行页
└── 系统管理
    ├── 用户管理
    └── 权限配置
```

5. 点击 **保存**，进入项目详情页。

### 2.2 邀请陈产品和叶产品加入项目

1. 在项目门户页右上角，点击 **「成员」** 按钮。
2. 在弹窗中搜索用户名：
   - 选择 `陈产品`，角色设为 **编辑者**，点击 **添加**。
   - 选择 `叶产品`，角色设为 **编辑者**，点击 **添加**。
3. 关闭弹窗。此时陈产品和叶产品都能在 **项目列表** 中看到「天宫平台」。

### 2.3 绑定原型到菜单

如果已有原型文件，Owner/Admin 可以先把它们挂到菜单上：

1. 在项目门户左侧菜单中，点击一个尚未绑定的菜单项，例如 **实体列表**。
2. 右侧会显示「该菜单项尚未绑定原型」。
3. 在下拉框中选择一个原型，点击 **绑定**。
4. 依次把 `实体详情`、`关系图谱`、`方法编辑器` 等菜单项都绑定好。

> 如果还没有原型文件，可以先跳过绑定，由陈产品/叶产品在签出后通过 Skill 上传。

---

## 三、日常协作流程：陈产品编辑实体建模模块

### 3.1 陈产品进入项目并签出模块

1. 陈产品登录后，点击顶部 **「项目」**，找到 **天宫平台**，点击进入。
2. 在项目门户左侧菜单中，选择 **实体列表**。
3. 右侧预览区上方会显示当前绑定的原型信息，以及一个 **「签出」** 按钮。
4. 点击 **「签出」**，该菜单项右侧会出现绿色标签 **「我签出」**。
5. 其他成员（包括叶产品）此时只能查看该模块，不能签出或上传覆盖。

### 3.2 本地修改并通过 Skill 上传

陈产品在本地对实体列表原型进行修改：

1. 打开本地代码或设计文件。
2. 修改完成后，使用 Kimi Code CLI 的 `fuxi-packager` Skill 打包并上传：

```bash
kimi skill fuxi-packager --project 天宫平台 --module 实体列表
```

> 如果 Skill 暂时不支持按项目/模块上传，也可以直接调用平台已有的原型上传接口，把 ZIP 更新到已签出的原型上。

3. 上传成功后，项目门户右侧预览会自动刷新（陈产品可手动刷新页面确认）。

### 3.3 签入释放锁

1. 陈产品确认修改无误后，在项目门户点击 **「签入」**。
2. 签出标签消失，其他成员可以继续签出该模块。
3. 每次签入会自动在项目版本历史中留下一条变更记录。

---

## 四、叶产品同时编辑方法建模模块

叶产品的操作与陈产品完全相同，只是负责不同菜单项：

1. 进入 **天宫平台** 项目门户。
2. 选择左侧菜单 **方法编辑器**。
3. 点击 **签出**。
4. 本地修改后通过 Skill 上传。
5. 点击 **签入**。

由于签出锁的粒度是 **菜单项级别**，陈产品和叶产品可以同时签出不同模块，互不干扰。

---

## 五、获取最新变更

当陈产品签入实体列表的更新后，叶产品如果想查看：

1. 叶产品刷新项目门户页面。
2. 点击左侧 **实体列表** 菜单项，右侧预览会加载陈产品更新后的最新版本。

> 目前需要手动刷新页面获取最新状态。后续版本会增加实时通知。

---

## 六、里程碑管理：创建快照

当项目达到一个重要节点（例如完成 V1.0 评审）时，Owner/Admin 可以创建快照：

1. 在项目门户右上角点击 **「快照」**。
2. 输入快照名称，例如 `V1.0 评审基线`。
3. 点击 **创建快照**。
4. 快照会保存当前项目的完整状态：
   - 菜单结构
   - 每个菜单项当前绑定的原型
   - 每个原型的当前版本号

如果后续改动出现问题，可以随时选择该快照点击 **恢复**，系统会自动：
- 还原菜单结构
- 还原每个菜单项的原型绑定
- 把每个原型回滚到快照时的版本

---

## 七、异常处理

### 7.1 有人忘记签入怎么办？

如果陈产品周五签出后忘记签入，周一叶产品需要编辑同一模块：

1. 叶产品点击签出时，系统会提示 **「该模块已被其他人签出」**。
2. 叶产品在群里 @ 陈产品提醒签入。
3. 如果陈产品暂时无法操作，Owner/Admin 可以在项目门户点击 **「强制释放」**，释放该签出锁。
4. 叶产品即可正常签出编辑。

> 注意：强制释放不会丢失已上传的文件，只是释放编辑锁。

### 7.2 签出超时自动释放

每个签出锁默认 **24 小时**后自动释放。如果陈产品签出后 24 小时内没有签入，系统会自动把他的签出状态标记为「已超时」，其他人可以重新签出。

### 7.3 想回到之前的版本

1. 点击项目门户右上角 **「快照」**。
2. 找到需要恢复的快照（例如 `V1.0 评审基线`）。
3. 点击 **恢复**。
4. 系统会提示确认，确认后项目会回到该快照状态。

---

## 八、给外部演示：统一的项目门户链接

项目创建后，会生成一个统一的访问链接：

```
http://<部署地址>/project/<项目ID>
```

例如：

```
http://192.168.2.145/project/mqqci4m5wyxhji
```

把这个链接发给领导、客户或测试人员，他们就能像使用一个完整的系统一样，在天宫平台的各个模块之间自由跳转预览。

---

## 九、注意事项

1. **签出锁是防止冲突的关键**：编辑任何模块前，务必先签出；编辑完成后，务必签入。
2. **不要长时间占用锁**：如果当天不做了，建议签入，让其他人可以继续工作。
3. **Owner/Admin 谨慎使用强制释放**：强制释放前应确认当前签出人没有未保存的本地修改。
4. **快照是安全网**：在重大改动前（例如重构整个菜单结构）先创建快照，方便回滚。
5. **成员角色按需分配**：
   - 需要参与编辑的 → 编辑者
   - 只需要查看评审的 → 查看者
   - 项目负责人 → 创建者/管理员

---

## 十、快速操作对照表

| 我想做… | 操作路径 |
|---|---|
| 创建新项目 | 项目 → 创建项目 |
| 配置菜单 | 进入项目 → 管理菜单 |
| 邀请成员 | 进入项目 → 成员 → 添加 |
| 绑定原型到菜单 | 进入项目 → 点击未绑定菜单项 → 选择原型 → 绑定 |
| 编辑某个模块 | 进入项目 → 点击菜单项 → 签出 → 修改上传 → 签入 |
| 查看别人更新 | 刷新页面 → 点击对应菜单项 |
| 保存里程碑 | 进入项目 → 快照 → 创建快照 |
| 回滚到之前状态 | 进入项目 → 快照 → 选择快照 → 恢复 |
| 释放他人签出锁 | 管理员/Owner → 点击已签出模块 → 强制释放 |
