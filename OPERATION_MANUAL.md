# 伏羲平台操作总览

> 面向第一次使用伏羲平台的产品、设计和研发同学。普通使用只需要知道“下一步做什么”，不需要先理解 MCP、内部任务模型或底层协议。

## 伏羲是做什么的

伏羲负责管理 AI 原型的创建结果、版本、项目协作、预览和分享。AI 对话仍发生在你平时使用的 AI 软件中，伏羲本身不提供独立的 AI 聊天窗口。

第一次使用时，只需要记住这条路径：

```text
接入你的 AI → 创建原型 → 回伏羲检查结果 → 需要时修改、协作或分享
```

## 主要入口

| 入口 | 什么时候用 |
|---|---|
| 原型列表 | 点击「让 AI 创建原型」、查找已有原型、进入原型详情 |
| 原型详情 | 看预览、版本和设计说明，点击「让 AI 修改」或生成分享链接 |
| 项目 | 管理项目菜单和成员，发起项目内修改并审核待审修订 |
| 接入平台 MCP | 页面上的接入入口；用于把伏羲接入你平时使用的 AI 软件 |
| 帮助 | 查找快速入门、操作手册和故障处理 |
| 系统管理 | 管理员维护平台配置、公告和帮助内容 |

## 第一次接入你的 AI

### 先准备一个 AI 软件

AI 软件就是你平时和 AI 对话的应用，例如 WorkBuddy 或 Cursor。

- **WorkBuddy**：推荐使用；
- **Cursor**：也可以使用；
- **Codex**：当前不提供自动接入流程。

如果电脑上还没有可用的 AI 软件，先按公司提供的安装方式准备 WorkBuddy。要完成伏羲的 AI 创建或修改流程，必须先有一个可用的 AI 软件。

### 按这几步完成接入

1. 登录伏羲，点击「接入平台 MCP」；
2. 选择你正在使用的 AI 软件；不确定时优先选 WorkBuddy；
3. 生成接入提示词并复制完整内容；
4. 打开刚才选择的 AI 软件，把提示词粘贴到新对话中并发送；
5. 等 AI 执行完成，按提示重新加载 AI 软件或重新打开一个新会话；
6. 在新会话里确认伏羲已经可用。

使用 WorkBuddy 时，接入完成后请到「连接器 → 自定义连接器」检查 `fuxi-platform` 是否已信任；即使没有弹出信任提示，也建议确认一次。

可以直接对 AI 说：

> 请确认现在可以使用伏羲，并告诉我连接是否正常。

看到 AI 明确报告连接正常，并且新会话可以继续执行伏羲相关任务，才算接入完成。不要把“文件下载完成”“安装程序执行结束”当成最终成功。

正常接入不要求你自己扫描目录、修改配置文件或拼装安装命令。接入失败时保留 AI 返回的完整错误，不要手工猜配置路径。

## 创建第一个原型

从「原型列表」开始，完整走一遍下面的路径：

1. 点击「让 AI 创建原型」；
2. 填写需求；
3. 生成并复制任务提示词；
4. 把任务提示词粘贴到已经接入伏羲的 AI 软件中；
5. 等 AI 完成交付；
6. 回到伏羲「原型列表」，找到刚生成的原型；
7. 进入原型详情并打开预览；
8. 按真实页面结果验收。

需求至少写清：

- 谁来使用；
- 用户要完成什么；
- 必须包含哪些页面；
- 关键操作是什么；
- 哪些内容这次不做；
- 怎样算完成。

最小示例：

```text
原型名称：客户管理
目标用户：销售人员
核心任务：筛选客户、查看客户详情、记录跟进结果
必须包含：客户列表、客户详情
关键操作：按跟进状态筛选；新增跟进记录
不做：客户导入、权限管理
验收：筛选“待跟进”客户，打开详情并新增一条跟进记录后，页面能看到最新记录
```

如果有需求文档，目标不是“必须提供某种路径”，而是**确保 AI 能读到它**：

- 本地文件可以提供完整文件路径；
- 如果 AI 读不到路径，而当前 AI 软件支持上传文件，就直接上传到当前会话；
- 仍无法读取时，把本次实现必需的内容粘贴到会话里，不要让 AI 猜需求。

### 怎么判断真的完成了

不要只看 AI 说“已生成”或“已上传”。至少看到：

- 「原型列表」里出现了目标原型；
- 原型详情能看到本次交付后的版本信息；
- 点击预览后页面能正常打开，没有空白页或明显报错；
- 本次要求的关键页面存在；
- 关键按钮、筛选、录入或跳转可以按需求操作。

## 修改原型

先判断原型是否已经进入项目。

### 修改独立原型

从原型详情点击「让 AI 修改」，写清：

- 要改什么；
- 必须保留什么；
- 修改后怎样验收。

把平台生成的提示词交给 AI。AI 完成后回到原型详情检查新的正式版本和预览。

### 修改项目内原型

进入项目中的目标原型，点击「让 AI 修改」。项目里的修改不会直接覆盖正式版，而是进入审核流程：

```text
发起修改 → AI 提交待审修订 → 负责人退回或采用 → 采用后形成新的正式版
```

AI 提交修订不代表正式版已经更新。只有负责人点击「采用为正式版」后，本次修改才进入正式版。

## 项目协作

项目负责人通常需要完成这些事情：

1. 创建项目并维护菜单；
2. 绑定已有原型；
3. 添加项目成员；
4. 从目标原型发起修改并确认负责人；
5. 审核待审修订，点击「退回」或「采用为正式版」。

任务页会区分：

- **正式版**：当前已经采用的结果；
- **待审修订**：AI 已提交、尚未采用的修改；
- **历史尝试**：之前被替换或退回的提交。

同一个任务任一时刻只保留一个当前待审修订。

## 预览、正式版和分享

- **预览**：检查页面是否能打开、交互是否符合预期；
- **正式版**：当前正式采用的版本；
- **待审修订**：等待负责人审核的项目修改；
- **分享**：给评审者一个受控预览入口，不授予平台管理权限。

分享时请发送平台生成的**完整分享链接**。如果对方打不开，让对方从完整链接重新打开，并在同一个正常浏览器中完成跳转；不要只复制跳转后的预览地址。

## 怎样向 AI 描述需求

不需要在提示词里手写伏羲底层工具名或内部参数。优先从伏羲页面发起任务，让平台补齐 AI 执行需要的上下文。

你真正需要补充的是：

```text
目标用户：
核心任务：
要做：
必须保留：
不做：
验收结果：
参考文件：
```

避免只说“优化一下”“做得高级一点”“你看着办”。把主观要求改成具体场景、约束和可观察结果，可以明显减少返工。

## 什么时候需要看进阶设置

如果你只是做普通原型，可以直接使用平台默认值，不需要先理解组件规范、运行时或输出模式。

只有在这些场景下再看《规范、运行时与输出模式》：

- 必须使用指定组件规范；
- 要证明某套组件被真实使用；
- 修改已有原型并保持原技术栈；
- AI 无法判断原有实现，需要人工确认。

## 常见问题

### AI 已经说“上传成功”，为什么还不能算完成？

上传只是中间状态。以伏羲里的目标原型、版本信息和真实预览为准。

### 项目里的修改已经提交，为什么正式版没变？

这是正常的。AI 提交的是待审修订，负责人点击「采用为正式版」后才会更新正式版。

### 为什么复制跳转后的预览地址会打不开？

请分享平台生成的完整分享链接，不要只复制跳转后的地址。

### 我没有权限怎么办？

联系项目负责人或平台管理员申请当前动作所需权限。不要借用账号或凭据，也不要用删除、回滚等高风险操作绕过权限。

## 出错时先做什么

优先保留：

- 当前原型和版本；
- 当前项目任务；
- AI 返回的完整错误；
- 你本来要完成的目标。

然后根据错误重新读取平台当前状态再处理。不要因为一次失败就连续重试、删除正式版本、强制覆盖或回滚。

## 帮助中心怎么选文章

第一次使用时不要一次看完所有文章，按当前任务往下走：

1. 第一次接入：看《接入你的 AI》；
2. 第一次创建：接入成功后再看《让 AI 创建原型》；
3. 要修改原型：看《让 AI 修改原型》；
4. 要做项目协作：看《项目协作：审核与采用修订》；
5. 要交付或分享：看《构建、校验、发布与分享》；
6. 不知道怎么描述需求：看《如何向 AI 描述原型需求》；
7. 遇到异常：看《常见问题》或《故障排查与安全边界》；
8. 只有需要控制组件实现时，再看《规范、运行时与输出模式》。

普通使用先解决当前任务；只有需要排障或控制实现细节时，再进入技术层。
