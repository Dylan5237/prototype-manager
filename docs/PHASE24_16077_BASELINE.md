# 阶段 24：16077 接入只读性能基线

> 日期：2026-09-01
> 原始报告：`.release/phase24-16077-baseline.json`（本地忽略目录）
> 运行方式：本地 Node MCP + 16077 测试 API；凭据由本地保护包装器注入，未写入报告

## 环境与样本

- 目标：`http://192.168.2.145:16077`
- 认证用户 ID：`14`
- 每条链路 10 次；健康、项目列表、项目详情、原型列表、MCP `check_connection` 均 10/10 成功。
- MCP `check_connection` 包含认证和运行时 heartbeat，因此不是纯 GET；未执行任何原型上传、版本写入或删除。

## 首轮结果（2026-09-01）

| 链路 | p50 | p95 | 最大 | 失败 |
|---|---:|---:|---:|---:|
| `/api/health` | 4.03 ms | 6.19 ms | 6.19 ms | 0 |
| 项目列表（分页/摘要） | 6.19 ms | 12.90 ms | 12.90 ms | 0 |
| 项目详情 | 10.80 ms | 33.15 ms | 33.15 ms | 0 |
| 原型列表（`scope=all` 分页） | 9.23 ms | 29.54 ms | 29.54 ms | 0 |
| MCP `check_connection` | 382.20 ms | 585.51 ms | 585.51 ms | 0 |

## 最终 release 复跑

最终 release `20260901-182503-da787bef` 切换后再次执行 10 次，全部通过：

| 链路 | p50 | p95 | 最大 | 失败 |
|---|---:|---:|---:|---:|
| `/api/health` | 3.01 ms | 5.07 ms | 5.07 ms | 0 |
| 项目列表（分页/摘要） | 5.30 ms | 13.31 ms | 13.31 ms | 0 |
| 项目详情 | 11.37 ms | 20.11 ms | 20.11 ms | 0 |
| 原型列表（`scope=all` 分页） | 8.05 ms | 10.53 ms | 10.53 ms | 0 |
| MCP `check_connection` | 300.03 ms | 344.81 ms | 344.81 ms | 0 |

## 当前判断

- 在本轮只读链路中，MCP 启动/认证/heartbeat 明显慢于普通 API，但尚未拆分到兑换、登录、工具发现和 heartbeat 各子阶段，不能直接归因。
- 项目列表和原型列表分页接口已具备可重复基线，可用于后续查询优化对照。
- 原型生成、ZIP 上传/解压/版本写入/回读、更新切换/回滚和真实浏览器页面仍是 `UNVERIFIED`；本报告不把只读成功写成端到端性能验收。
