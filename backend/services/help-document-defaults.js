const HELP_DOCUMENT_DEFAULTS = [
  {
    slug: 'quick-start',
    title: '伏羲平台快速入门',
    summary: '第一次使用伏羲时，只看这篇：接入你的 AI、创建原型、检查结果。',
    version: '1.2',
    sortOrder: 10,
    categorySlugs: ['beginner', 'beginner-platform', 'beginner-ai', 'beginner-platform-account', 'beginner-ai-create', 'beginner-platform-publish'],
    contentMarkdown: `## 先准备一个 AI 软件

AI 软件就是你平时和 AI 对话的应用，例如 WorkBuddy 或 Cursor。

- **WorkBuddy**：推荐使用；
- **Cursor**：也可以使用；
- **Codex**：当前不提供自动接入流程。

如果电脑上还没有可用的 AI 软件，先按公司提供的安装方式准备 WorkBuddy。伏羲本身不提供独立的 AI 聊天窗口。

## 第一步：接入你的 AI

1. 登录伏羲平台，点击「接入平台 MCP」；
2. 选择你正在使用的 AI 软件；不确定时优先选 WorkBuddy；
3. 生成接入提示词并复制完整内容；
4. 打开刚才选择的 AI 软件，把提示词粘贴到新对话中并发送；
5. 等 AI 执行完成，按提示重新加载 AI 软件或打开新会话；
6. 在新会话里确认伏羲已经可以使用。

使用 WorkBuddy 时，接入完成后请到「连接器 → 自定义连接器」检查 **fuxi-platform** 是否已信任；即使没有弹出信任提示，也建议确认一次。

可以直接对 AI 说：

> 请确认现在可以使用伏羲，并告诉我连接是否正常。

看到 AI 明确报告连接正常，并且新会话可以继续执行伏羲相关任务，才算接入完成。

## 第二步：创建第一个原型

1. 回到「原型列表」，点击「让 AI 创建原型」；
2. 填写要解决的问题、目标用户、页面和关键操作；
3. 生成并复制任务提示词；
4. 把任务提示词粘贴到已经接入伏羲的 AI 软件中；
5. 等 AI 完成交付；
6. 回到伏羲「原型列表」，找到刚生成的原型；
7. 进入原型详情并打开预览。

最小填写示例：

~~~text
原型名称：客户管理
目标用户：销售人员
核心任务：筛选客户、查看客户详情、记录跟进结果
必须包含：客户列表、客户详情
关键操作：按跟进状态筛选；新增跟进记录
不做：客户导入、权限管理
验收：筛选“待跟进”客户，打开详情并新增一条跟进记录后，页面能看到最新记录
~~~

如果有需求文档，重点是**确保 AI 能读到它**。本地文件可以提供完整路径；AI 读不到时，如果当前 AI 软件支持上传文件，就直接上传到会话；仍无法读取时，把本次实现必需的内容粘贴到会话里。

## 第三步：检查结果

不要只看 AI 说“已生成”或“已上传”。至少确认：

- 「原型列表」里出现了目标原型；
- 原型详情能看到本次交付后的版本信息；
- 点击预览后页面能正常打开，没有空白页或明显报错；
- 本次要求的关键页面存在；
- 关键按钮、筛选、录入或跳转可以按需求操作。

## 接下来可以做什么

- 修改独立原型：从原型详情点击「让 AI 修改」；
- 修改项目里的原型：进入项目中的目标原型，点击「让 AI 修改」；
- 给同事评审：从原型详情生成完整分享链接。

遇到问题时，先保留 AI 返回的完整错误和当前原型，不要连续重试、删除或回滚。`
  },
  {
    slug: 'mcp-onboarding',
    title: '接入你的 AI',
    summary: '把伏羲接入你平时使用的 AI 软件，并确认新会话真的可用。',
    version: '1.2',
    sortOrder: 20,
    categorySlugs: ['beginner', 'beginner-platform', 'beginner-platform-mcp'],
    contentMarkdown: `## 先知道你在选择什么

页面里的 AI 软件，就是你平时和 AI 对话的应用。

- **WorkBuddy**：推荐使用；
- **Cursor**：也可以使用；
- **Codex**：当前不提供自动接入流程。

不确定选哪个时，优先选择 WorkBuddy。不要因为看到陌生的技术名称就自己猜配置方式。

## 按这几步接入

1. 在伏羲点击「接入平台 MCP」；
2. 选择你正在使用的 AI 软件；
3. 点击生成接入提示词；
4. 复制平台生成的**完整提示词**；
5. 打开刚才选择的 AI 软件，新建对话，把提示词粘贴进去并发送；
6. 等 AI 执行完成；
7. 按 AI 提示重新加载软件或打开新会话；
8. 在新会话里重新确认伏羲是否可用。

安装、下载、校验和配置由接入程序完成。你不需要自己扫描目录、修改配置文件或拼装替代安装命令。

## 使用 WorkBuddy 时多检查一步

接入完成后，请到：

**连接器 → 自定义连接器 → fuxi-platform**

确认它已经被信任。即使 WorkBuddy 没有主动弹出信任提示，也建议检查一次。

## 怎么判断接入成功

可以在重新加载后的新会话里直接说：

> 请确认现在可以使用伏羲，并告诉我连接是否正常。

成功时至少应该看到：

- AI 明确报告伏羲连接正常；
- 新会话可以识别伏羲相关能力；
- 可以继续执行创建、读取或修改原型的任务。

“文件已经下载”“安装程序执行结束”或“旧会话还能用”都不能单独证明接入完成。

## 接入失败怎么办

先保留 AI 返回的完整失败信息，再按现象处理：

- **选错 AI 软件**：回伏羲重新生成对应软件的接入提示词；
- **下载或校验失败**：重新从平台生成一次接入提示词再试；
- **权限不足**：按 AI 软件提示授予必要权限；
- **连接信息过期**：重新从伏羲生成一次接入提示词；
- **安装完成但新会话仍看不到伏羲**：重新加载或重启 AI 软件后再验证。

不要手工猜配置路径，也不要把连接码、密码或完整凭据内容粘贴到聊天、日志或业务仓库。`
  },
  {
    slug: 'platform-basics',
    title: '伏羲平台操作总览',
    summary: '快速认识创建、修改、项目协作、预览和分享分别在哪里完成。',
    version: '1.3',
    sortOrder: 25,
    categorySlugs: ['beginner', 'beginner-platform', 'beginner-platform-account', 'beginner-platform-publish'],
    contentMarkdown: `## 伏羲是做什么的

伏羲负责管理 AI 原型的创建结果、版本、项目协作、预览和分享。AI 对话仍发生在你平时使用的 AI 软件中，伏羲本身不提供独立的 AI 聊天窗口。

第一次使用时，只需要记住这条路径：

~~~text
接入你的 AI → 创建原型 → 回伏羲检查结果 → 需要时修改、协作或分享
~~~

## 主要入口

| 入口 | 什么时候用 |
|---|---|
| 原型列表 | 点击「让 AI 创建原型」、查找已有原型、进入原型详情 |
| 原型详情 | 看预览、版本和设计说明，点击「让 AI 修改」或生成分享链接 |
| 项目 | 管理项目菜单和成员，发起项目内修改并审核待审修订 |
| 接入平台 MCP | 页面上的接入入口；用于把伏羲接入你平时使用的 AI 软件 |
| 帮助 | 查找快速入门、操作手册和故障处理 |
| 系统管理 | 管理员维护平台配置、公告和帮助内容 |

## 创建原型

从「原型列表 → 让 AI 创建原型」开始。你负责描述需求和验收结果，平台负责生成任务提示词，AI 负责实现和交付。

详细步骤请看《让 AI 创建原型》。

## 修改原型

先判断原型是否已经进入项目：

- **独立原型**：在原型详情直接点击「让 AI 修改」；
- **项目内原型**：进入项目中的目标原型点击「让 AI 修改」，AI 提交后由负责人审核。

详细步骤请看《让 AI 修改原型》和《项目协作：审核与采用修订》。

## 预览、正式版和分享

- **预览**：检查页面能否打开、交互是否符合预期；
- **正式版**：当前正式采用的结果；
- **待审修订**：项目任务中等待负责人审核的修改，不会自动覆盖正式版；
- **分享**：给评审者一个受控预览入口，不授予平台管理权限。

## 普通使用不需要先学什么

普通使用不要求你理解底层连接协议、内部任务模型、运行时配置或分享协议。平台生成的任务提示词会带上 AI 执行所需的信息。

只有在需要控制组件规范、排查失败或进行平台管理时，再阅读对应的进阶手册。`
  },
  {
    slug: 'create-prototype',
    title: '让 AI 创建原型',
    summary: '从填写需求到打开预览，完整走通一次 AI 原型创建。',
    version: '1.2',
    sortOrder: 30,
    categorySlugs: ['beginner', 'beginner-ai-create', 'beginner-ai'],
    contentMarkdown: `## 完整操作路径

1. 进入「原型列表」；
2. 点击「让 AI 创建原型」；
3. 填写需求和验收标准；
4. 如果页面提供实现方式选项，普通原型直接使用默认或快速验证方式即可；
5. 生成并复制任务提示词；
6. 打开已经接入伏羲的 AI 软件，把任务提示词粘贴进去并发送；
7. 等 AI 完成实现和交付；
8. 回到伏羲「原型列表」，找到刚生成的原型；
9. 进入原型详情并打开预览；
10. 按真实页面结果验收。

## 需求至少写清什么

- **目标用户**：谁会使用；
- **核心任务**：用户要完成什么；
- **必须页面**：本次一定要有的页面；
- **关键操作**：点击、筛选、录入、提交等；
- **不在范围内**：这次明确不做什么；
- **验收结果**：打开哪里、执行什么操作、应该看到什么。

最小示例：

~~~text
原型名称：客户管理
目标用户：销售人员
核心任务：筛选客户、查看客户详情、记录跟进结果
必须包含：客户列表、客户详情
关键操作：按跟进状态筛选；新增跟进记录
不做：客户导入、权限管理
验收：筛选“待跟进”客户，打开详情并新增一条跟进记录后，页面能看到最新记录
~~~

## 有需求文档时怎么提供

目标只有一个：**确保 AI 能读到需求**。

- 本地文件可以提供完整文件路径；
- 如果 AI 读不到，而当前 AI 软件支持文件上传，就把文件直接上传到当前会话；
- 如果仍无法读取，把这次实现必需的内容粘贴到会话里；
- 不要只给一个模糊文件名，然后让 AI 猜文件在哪里。

## 怎么判断创建完成

AI 报告完成后，回伏羲检查：

1. 「原型列表」里是否出现目标原型；
2. 原型详情是否能看到本次交付后的版本信息；
3. 点击预览后是否能正常打开，没有空白页或明显报错；
4. 本次要求的关键页面是否存在；
5. 关键按钮、筛选、录入或跳转是否可以按需求操作。

只有平台已经拿到结果并且真实预览可用，才算创建完成。

## 如果结果不满意

直接描述“哪里不对、希望变成什么、哪些内容必须保留”，再从原型详情点击「让 AI 修改」。不要为了重做而先删除当前正式版。`
  },
  {
    slug: 'modify-prototype',
    title: '让 AI 修改原型',
    summary: '先判断是独立原型还是项目内原型，再走对应的修改和审核流程。',
    version: '1.2',
    sortOrder: 40,
    categorySlugs: ['advanced-ai-quality', 'beginner-ai', 'advanced-ai'],
    contentMarkdown: `## 第一步：判断原型归属

- **未绑定项目的独立原型**：从原型详情点击「让 AI 修改」；
- **已经进入项目的原型**：进入项目中的目标原型点击「让 AI 修改」。

这两条路径不要混用。项目里的修改需要经过负责人审核，不能直接覆盖正式版。

## 修改独立原型

1. 打开目标原型详情；
2. 点击「让 AI 修改」；
3. 写清本次要改什么、必须保留什么、怎样算改好；
4. 生成并复制平台提示词；
5. 把提示词交给已经接入伏羲的 AI 软件；
6. AI 完成后，回到原型详情检查新的正式版和预览。

出现版本冲突、目标不一致或预览失败时，不要强制覆盖，先重新读取当前原型再处理。

## 修改项目内原型

1. 进入项目并选择目标原型；
2. 点击「让 AI 修改」；
3. 确认负责人和需求；
4. 生成并复制任务提示词；
5. 把任务提示词交给 AI；
6. AI 提交后，负责人比较“正式版”和“待审修订”；
7. 负责人点击「退回」继续修改，或点击「采用为正式版」完成审核。

提交待审修订不等于正式版已经更新；只有负责人采用后才会更新正式版。

## 修改需求怎么写更有效

尽量包含三类信息：

- **修改范围**：具体页面、区域或交互；
- **保留范围**：哪些内容不要动；
- **验收结果**：修改后你要看到的可观察结果。

例如：“把客户列表的筛选区改成两行布局，保留现有字段和表格列；在 1280px 宽度下筛选项不溢出，查询和重置仍能正常使用。”`
  },
  {
    slug: 'prototype-delivery',
    title: '构建、校验、发布与分享',
    summary: '理解 AI 什么时候才算真正交付完成，以及预览、正式版和分享有什么区别。',
    version: '1.2',
    sortOrder: 45,
    categorySlugs: ['advanced-ai-quality', 'advanced-ai', 'advanced-platform-safety', 'beginner-platform-publish'],
    contentMarkdown: `## 先记住：AI 说“完成”不等于平台已经完成

一次原型交付至少要经过：

~~~text
本地实现 → 构建与校验 → 上传伏羲 → 平台回读 → 浏览器验收
~~~

普通用户不需要手工执行这些技术步骤，但应该用平台里的真实结果判断是否真的完成。

## 交付完成要检查什么

至少确认：

- 「原型列表」或目标详情中能找到正确原型；
- 原型详情显示本次交付后的版本信息；
- 预览可以打开，没有空白页或明显报错；
- 关键页面存在；
- 本次要求的关键操作可以真实使用。

“构建通过”“文件已上传”或“接口返回成功”都只是中间状态。

## 预览、正式版、待审修订

- **预览**：你现在看到的页面是否能正常运行；
- **正式版**：当前正式采用的版本；
- **待审修订**：AI 为项目任务提交的修改，负责人采用前不会影响正式版。

因此，在项目里看到修订已经提交，不代表正式版已经改变。

## 分享给别人

从原型详情生成完整分享链接，把这个链接直接发给评审者即可。分享只提供被分享内容的预览能力，不授予平台管理权限。

如果对方打不开，请让对方从**完整分享链接**重新打开，并使用同一个正常浏览器完成跳转；不要只复制浏览器跳转后的预览地址。

## 给排障人员的执行边界

排查交付问题时，应按“构建 → 校验 → 打包 → 上传 → 平台回读 → 浏览器验收”的顺序定位失败阶段。出现部分失败或版本冲突时，先读取平台当前状态，不要盲目重复上传。`
  },
  {
    slug: 'faq',
    title: '常见问题',
    summary: '快速回答接入、修改、项目审核、预览、分享和权限中的常见疑问。',
    version: '1.2',
    sortOrder: 50,
    categorySlugs: ['beginner-platform', 'advanced-platform', 'advanced-platform-safety'],
    contentMarkdown: `## 应该选择哪个 AI 软件？

优先使用 WorkBuddy；Cursor 也可以使用。当前 Codex 不提供自动接入流程。不确定时选 WorkBuddy，不要自己猜配置路径。

## WorkBuddy 接入后还需要做什么？

到「连接器 → 自定义连接器」检查 **fuxi-platform** 是否已信任。即使没有弹出信任提示，也建议确认一次。

## 为什么接入后还要重新加载或新开会话？

AI 软件通常会在启动或新会话建立时加载新的连接能力。接入完成后，旧会话可能还在使用旧状态，所以需要重新加载或打开新会话再确认。

## 怎么确认接入真的成功？

在新会话里直接说：“请确认现在可以使用伏羲，并告诉我连接是否正常。”

只有 AI 明确报告连接正常，并且能继续执行伏羲任务，才算接入完成。

## AI 已经说“上传成功”，为什么还不能算完成？

因为上传只是交付的一部分。你还需要在伏羲里看到目标原型、版本信息和可用预览。以平台里的真实页面为准。

## 项目里的修订已经提交，为什么正式版没变？

这是正常的。项目修改先形成“待审修订”，负责人点击「采用为正式版」后才会更新正式版。负责人也可以点击「退回」，让 AI 继续修改后再次提交。

## 为什么复制浏览器跳转后的预览地址会打不开？

请分享平台生成的**完整分享链接**，不要只复制跳转后的地址。让访问者在同一个正常浏览器里从完整链接打开即可。

## 我没有权限怎么办？

查看、修改、审核、发布、分享等权限可能不同。联系项目负责人或平台管理员申请当前动作所需权限，不要借用账号或凭据，也不要用删除、回滚等高风险操作绕过权限。

## 原型预览打不开怎么办？

先保留当前版本和 AI 返回的错误，然后查看《故障排查与安全边界》。不要先删除版本再重做。`
  },
  {
    slug: 'project-collaboration',
    title: '项目协作：审核与采用修订',
    summary: '用“发起修改 → 待审修订 → 负责人审核 → 正式版”完成项目内多人协作。',
    version: '1.2',
    sortOrder: 55,
    categorySlugs: ['advanced-platform', 'advanced-platform-project', 'advanced', 'advanced-platform-safety'],
    contentMarkdown: `## 项目协作的核心模型

项目里的修改不会让 AI 直接覆盖正式版，而是走一条可审核的流程：

~~~text
发起修改 → AI 提交待审修订 → 负责人退回或采用 → 采用后形成新的正式版
~~~

这样可以让多人协作时始终保留一个明确的正式版。

## 项目负责人先准备什么

通常需要：

1. 创建项目并维护菜单；
2. 把已有原型绑定到项目；
3. 添加项目成员；
4. 从需要修改的目标原型点击「让 AI 修改」；
5. 确认本次修改的负责人和需求。

## 负责修改的人怎么参与

1. 打开分配给自己的项目任务；
2. 确认需求和验收标准；
3. 生成并复制平台任务提示词；
4. 把提示词交给已经接入伏羲的 AI 软件；
5. AI 完成后提交待审修订；
6. 等待负责人审核；被退回时按退回意见继续修改。

## 负责人怎么审核

任务页会突出展示：

- **正式版**：项目现在正在使用的版本；
- **待审修订**：AI 本次提交、尚未采用的修改；
- **历史尝试**：之前被替换或退回的提交记录。

负责人可以：

- 点击「退回」：写明需要继续修改的地方；
- 点击「采用为正式版」：确认本次修订并形成新的正式版。

同一个任务任一时刻只保留一个当前待审修订，新的提交会把之前的尝试留在历史中。

## 用户只需要记住的边界

- AI 提交待审修订，不会直接覆盖正式版；
- 负责人采用后才会更新正式版；
- 被退回后可以继续修改并再次提交；
- 快照、回滚、强制释放等高风险操作不是普通项目修改步骤。`
  },
  {
    slug: 'spec-and-quality',
    title: '规范、运行时与输出模式',
    summary: '需要控制组件规范或实现方式时再看这篇；普通原型可以直接使用平台默认值。',
    version: '1.2',
    sortOrder: 65,
    categorySlugs: ['advanced-ai-quality', 'advanced', 'advanced-ai', 'advanced-ai-profile'],
    contentMarkdown: `> 如果你只是使用伏羲生成普通原型，不需要先理解这一页。使用平台默认值即可。

## 三个概念分别解决什么问题

- **原型规范（prototype spec）**：规定原型应该遵循哪套设计和组件规范；
- **运行时配置（runtime profile）**：规定实际使用哪套前端运行时和组件实现；
- **输出模式**：规定这次更偏“快速验证”还是“严格证明组件实现”。

一次任务只使用一套明确的组合，避免不同规范和组件库混用。

## 什么时候需要你选择

- 有明确的公司或项目组件规范时；
- 需要证明原型真实使用指定组件时；
- 修改已有原型，必须保持原技术栈时；
- AI 无法判断原有实现，需要你确认时。

没有这些要求时，优先使用平台默认值。

## 两种输出模式

- **alignment**：适合快速验证需求、布局、密度、状态和关键交互；
- **implementation-proof**：适合需要严格证明所选组件规范被真实使用的场景。

如果指定组件没有对应能力，AI 应明确说明差距，而不是做一个“看起来像”的替代品冒充。

## 修改已有原型

已有原型应优先保持原有技术栈和组件规范。如果 AI 无法从现有代码、依赖或设计说明判断，应停止并向你确认，不要自行切换组件库。

## 最终仍以页面结果为准

无论选择哪种模式，都需要完成构建、平台交付和真实页面验收。静态检查通过不能替代浏览器中的实际效果。`
  },
  {
    slug: 'prompt-recipes',
    title: '如何向 AI 描述原型需求',
    summary: '不用学习底层工具调用；把目标、范围、约束和验收结果说清楚即可。',
    version: '1.2',
    sortOrder: 75,
    categorySlugs: ['advanced-ai-quality', 'beginner-ai', 'advanced-ai', 'advanced-ai-profile'],
    contentMarkdown: `## 先记住一个原则

你不需要告诉 AI 应该调用哪些伏羲底层工具，也不需要自己填写内部执行参数。优先从伏羲页面发起任务，让平台生成完整提示词。

你真正需要补充的是：**要解决什么问题、改什么、不改什么、怎样算完成。**

## 创建新原型

可以按这个结构描述：

~~~text
原型名称：
目标用户：
用户要完成的核心任务：
必须包含的页面：
关键操作：
需要覆盖的状态：
不在本次范围内：
验收结果：
参考文件/需求文档：
~~~

例如：

~~~text
创建一个“客户管理”原型。
目标用户：销售人员。
核心任务：筛选客户、查看客户详情、记录跟进结果。
必须包含：客户列表、客户详情。
关键操作：按跟进状态筛选；新增跟进记录。
不做：客户导入和权限管理。
验收：从列表筛选“待跟进”客户，打开详情并新增一条跟进记录后能看到最新记录。
需求文件：<确保 AI 能读取的文件或内容>
~~~

## 需求文件怎么提供

重点不是某一种路径格式，而是确保 AI 能读到：

- 本地文件可以提供完整路径；
- 当前 AI 软件支持上传时，可以直接上传文件；
- AI 无法读取时，把本次实现必需的内容粘贴到会话里；
- 不要只给模糊文件名，也不要让 AI 猜文件位置或需求内容。

## 修改独立原型

重点写三件事：

~~~text
要改：
必须保留：
验收结果：
~~~

例如：

~~~text
要改：把客户列表筛选区从一行改成两行，并压缩顶部留白。
必须保留：现有筛选字段、表格列和详情跳转。
验收：1280px 宽度下筛选项不溢出，点击“查询”和“重置”行为保持不变。
~~~

## 修改项目里的原型

从项目中的目标原型点击「让 AI 修改」后，只需要补充业务需求和验收标准。平台会自动补齐 AI 执行需要的项目和版本上下文。

例如：

~~~text
修改目标：订单详情页增加“异常原因”展示。
必须保留：现有订单状态和操作按钮。
验收：异常订单显示异常原因；正常订单不出现该区域。
~~~

AI 提交后等待负责人审核，不需要在提示词里手工描述底层审核协议。

## 什么样的要求最容易返工

- “做得高级一点”
- “优化一下交互”
- “参考常见后台”
- “你看着办”

这些描述缺少可观察结果。尽量把主观要求改成具体场景、约束和验收标准。`
  },
  {
    slug: 'troubleshooting',
    title: '故障排查与安全边界',
    summary: '按“现象 → 先做什么 → 不要做什么”排查接入、交付、项目修改和分享问题。',
    version: '1.2',
    sortOrder: 85,
    categorySlugs: ['advanced-platform', 'advanced-ai-quality', 'beginner-platform-mcp', 'advanced-platform-safety', 'beginner-platform-publish'],
    contentMarkdown: `## 接入后 AI 看不到伏羲

先做：

1. 确认接入时选择的是你当前正在使用的 AI 软件；
2. 重新加载 AI 软件或打开新会话；
3. 如果使用 WorkBuddy，到「连接器 → 自定义连接器」确认 **fuxi-platform** 已信任；
4. 在新会话里让 AI 再次确认伏羲连接是否正常。

如果仍失败，重新从伏羲生成一次对应 AI 软件的接入提示词，并保留 AI 返回的完整失败信息。

不要做：

- 不要扫描多个目录猜配置；
- 不要自己拼装替代安装命令；
- 不要手工修改未知配置文件；
- 不要把“文件已经存在”当成“AI 软件已经加载成功”。

## 原型构建或上传失败

先保留 AI 返回的完整错误。让 AI 根据失败阶段修复后重新构建和校验。

不要因为一次失败就删除当前正式版，也不要连续重复上传碰运气。

## AI 说完成，但伏羲里看不到正确结果

以伏羲为准，检查：

- 「原型列表」或目标详情里是否能找到正确原型；
- 原型详情是否显示本次交付后的版本信息；
- 预览是否能打开，没有空白页或明显报错；
- 关键页面和操作是否符合本次需求；
- 设计说明是否与本次修改一致。

如果 AI 报告“部分交付失败”或版本冲突，让它先读取平台当前状态，再决定是否重试。

## 项目里的修改提交后正式版没变

这是正常的。AI 提交的是待审修订，负责人点击「采用为正式版」后才会更新正式版。

如果 AI 提示使用旧版项目修改方式或要求额外走一套旧流程，请回到项目中的目标原型，重新点击「让 AI 修改」生成当前提示词。

## 分享链接打不开

让访问者从平台生成的**完整分享链接**重新打开，并在同一个正常浏览器中完成跳转。不要只复制浏览器跳转后的预览地址。

## 权限不足

查看、修改、审核、发布、分享、删除和回滚可能需要不同权限。联系项目负责人或平台管理员申请当前动作需要的权限。

## 哪些操作要特别谨慎

删除原型、回滚版本、恢复项目快照、强制释放等操作会改变已有状态。普通创建和修改任务不应该自动执行这些操作。

遇到不确定情况时，优先保留当前版本、任务和错误信息，再继续排查。`
  }
];

module.exports = { HELP_DOCUMENT_DEFAULTS };
