const HELP_DOCUMENT_DEFAULTS = [
  {
    slug: 'quick-start',
    title: '伏羲平台快速入门',
    summary: '第一次使用伏羲时，只看这篇：接入 AI、创建原型、检查结果。',
    version: '1.2',
    sortOrder: 10,
    categorySlugs: ['beginner', 'beginner-platform', 'beginner-ai', 'beginner-platform-account', 'beginner-ai-create', 'beginner-platform-publish'],
    contentMarkdown: `## 先完成一次接入

1. 登录伏羲平台，点击「接入平台 MCP」。
2. 选择你正在使用的 AI 工具。推荐 WorkBuddy，也可以选择 Cursor。
3. 把平台生成的完整提示词直接交给 AI，不要自己改写安装步骤。
4. 按提示重新加载 AI 工具或打开新会话。
5. 让 AI 确认伏羲已经可以正常使用。

> 当前 Codex 不提供确定性自动安装。如果页面没有把某个 AI 工具列为可用 Host，不要自行猜配置路径。

## 创建第一个原型

1. 回到「原型列表」，点击「让 AI 创建原型」。
2. 写清楚你要解决什么问题、谁来使用、需要哪些页面和关键操作。
3. 如果有需求文档，提供完整文件路径。
4. 把平台生成的任务提示词交给 AI。
5. AI 完成后，回到伏羲检查预览、版本和设计说明。

## 怎么判断真的完成了

不要只看 AI 说“已生成”或“已上传”。至少确认：

- 伏羲里出现了目标原型；
- 预览可以打开；
- 关键页面和操作符合需求；
- 版本和本次修改一致。

## 接下来可以做什么

- 修改自己的独立原型：从原型详情点击「让 AI 修改」；
- 修改项目里的原型：进入项目，从目标节点发起修改任务；
- 给同事评审：从原型详情生成分享链接。

遇到问题时，先保留 AI 返回的完整错误和当前原型，不要连续重试、删除或回滚。`
  },
  {
    slug: 'mcp-onboarding',
    title: '接入平台 MCP',
    summary: '选择正确的 AI Host，用平台生成的提示词完成接入，并确认新会话真的可用。',
    version: '1.2',
    sortOrder: 20,
    categorySlugs: ['beginner', 'beginner-platform', 'beginner-platform-mcp'],
    contentMarkdown: `## 你要做的只有三件事

1. 在伏羲点击「接入平台 MCP」并选择当前 AI Host；
2. 把平台生成的完整提示词交给 AI 执行；
3. 按提示重新加载客户端或打开新会话，再确认伏羲工具可用。

安装、下载、校验和配置由接入程序完成。不要让 AI 自己扫描多个客户端目录、拼装安装命令或改写平台给出的接入流程。

## Host 怎么选

- **WorkBuddy**：推荐，稳定支持；
- **Cursor**：兼容支持；
- **Codex**：当前不提供确定性自动安装；
- **其他 Host**：只在能够明确确认配置方式时继续，不猜测。

如果 WorkBuddy 提示需要信任自定义连接器，请在「连接器 → 自定义连接器」中信任 \`fuxi-platform\`。

## 怎么判断接入成功

接入程序完成后，必须在重新加载后的客户端或新会话里再次验证。你只需要关注最终结果：

- AI 能发现伏羲工具；
- AI 报告连接验证成功；
- 新会话可以继续执行伏羲创建、读取或修改任务。

如果 AI 报告 \`authentication=verified\`，说明连接认证已经通过。文件下载成功、配置文件写入或旧会话还能调用工具，都不能单独证明接入完成。

## 接入失败怎么办

优先按 AI 返回的失败阶段处理：

- **Host 不支持或配置路径无法确认**：停止，不要手工猜路径；
- **下载或校验失败**：重新从平台生成接入提示词后再试；
- **权限不足**：按客户端要求授予必要权限；
- **连接码过期**：重新从伏羲生成一次接入提示词；
- **安装完成但新会话没有伏羲工具**：重新加载或重启客户端后再验证。

连接码、token、密码和完整凭据内容不要粘贴到聊天、日志或业务仓库。`
  },
  {
    slug: 'platform-basics',
    title: '伏羲平台操作总览',
    summary: '快速认识原型、项目、接入、修改、审核和分享分别在哪里完成。',
    version: '1.3',
    sortOrder: 25,
    categorySlugs: ['beginner', 'beginner-platform', 'beginner-platform-account', 'beginner-platform-publish'],
    contentMarkdown: `## 伏羲是做什么的

伏羲负责管理 AI 原型的创建结果、版本、项目协作、预览和分享。AI 对话仍发生在你选择的 AI 工具中，伏羲不提供独立的 AI 聊天窗口。

第一次使用时，只需要记住这条路径：

\`\`\`text
接入 AI → 创建原型 → 回伏羲检查结果 → 需要时修改、协作或分享
\`\`\`

## 主要入口

| 入口 | 什么时候用 |
|---|---|
| 原型列表 | 创建原型、查找已有原型、进入原型详情 |
| 原型详情 | 看预览、版本、设计说明，发起独立原型修改或分享 |
| 项目 | 管理项目菜单和成员，发起项目内修改任务并审核修订 |
| 接入平台 MCP | 第一次把伏羲接入 WorkBuddy、Cursor 等 AI 工具 |
| 帮助 | 查找操作手册和故障处理 |
| 系统管理 | 管理员维护平台配置、公告和帮助内容 |

## 创建原型

从「原型列表 → 让 AI 创建原型」开始。你负责描述需求和验收结果，平台负责生成任务提示词，AI 负责实现和交付。

详细步骤请看《让 AI 创建原型》。

## 修改原型

先判断原型是否已经进入项目：

- **独立原型**：在原型详情直接点击「让 AI 修改」；
- **项目内原型**：进入项目目标节点发起修改任务，由负责人审核后才能成为正式版本。

详细步骤请看《让 AI 修改原型》和《项目协作：Task v2 与候选审核》。

## 预览、正式版本和分享

- **预览**：检查页面能否打开、交互是否符合预期；
- **正式版本**：当前被项目或原型详情采用的结果；
- **待审修订**：项目任务中等待负责人审核的修改，不会自动覆盖正式版；
- **分享**：给评审者一个受控预览入口，不授予平台管理权限。

## 不需要先学的内容

普通使用不要求你理解 MCP 工具名、任务 ID、候选 ID、runtime profile 或底层分享协议。平台生成的任务提示词会带上 AI 执行所需的信息。

只有在需要控制组件规范、排查失败或进行平台管理时，再阅读对应的进阶手册。`
  },
  {
    slug: 'create-prototype',
    title: '让 AI 创建原型',
    summary: '把业务需求说清楚，让 AI 生成可预览、可评审的伏羲原型。',
    version: '1.2',
    sortOrder: 30,
    categorySlugs: ['beginner', 'beginner-ai-create', 'beginner-ai'],
    contentMarkdown: `## 第一步：把需求说清楚

在「原型列表 → 让 AI 创建原型」中，至少说明：

- **目标用户**：谁会使用；
- **核心任务**：用户要完成什么；
- **必须页面**：本次一定要有的页面；
- **关键操作**：点击、筛选、录入、提交等；
- **验收结果**：打开哪里、执行什么操作、应该看到什么；
- **不在范围内**：这次明确不做什么。

如果有需求文档，提供完整本地路径，让 AI 先读取再开始实现。

## 第二步：选择实现方式

如果页面提供原型规范或实现模式，按本次目标选择即可：

- 想先快速验证需求和交互：选择偏快速验证的模式；
- 需要严格使用指定组件规范：选择对应规范和严格实现模式。

如果你只是做普通原型，不需要先理解 spec、runtime profile 等技术概念；平台生成的任务提示词会把必要信息交给 AI。

## 第三步：把平台提示词交给 AI

不要重新手写一套执行流程。把伏羲生成的完整提示词交给已经接入的平台 AI，让它按任务要求完成实现、构建、校验和交付。

## 第四步：回伏羲验收

AI 报告完成后，回到原型详情检查：

1. 目标原型是否存在；
2. 预览是否能正常打开；
3. 关键页面和交互是否符合需求；
4. 空状态、加载状态、错误状态等是否合理；
5. 设计说明和版本是否与本次交付一致。

只有平台已经拿到结果并且真实预览可用，才算创建完成。

## 如果结果不满意

直接描述“哪里不对、希望变成什么、哪些内容必须保留”，再从原型详情发起修改。不要为了重做而先删除正式版本。`
  },
  {
    slug: 'modify-prototype',
    title: '让 AI 修改原型',
    summary: '先判断是独立原型还是项目原型，再走对应修改和审核流程。',
    version: '1.2',
    sortOrder: 40,
    categorySlugs: ['advanced-ai-quality', 'beginner-ai', 'advanced-ai'],
    contentMarkdown: `## 第一步：判断原型归属

- **未绑定项目的独立原型**：从原型详情点击「让 AI 修改」；
- **已经绑定项目的原型**：进入对应项目节点发起修改任务。

这两条路径不要混用。项目里的修改必须经过项目负责人审核，不能用独立修改直接覆盖正式版。

## 修改独立原型

1. 打开目标原型详情；
2. 点击「让 AI 修改」；
3. 写清本次要改什么、必须保留什么、怎样算改好；
4. 把平台生成的完整提示词交给 AI；
5. AI 完成后，回到原型详情检查新版本和正式预览。

独立原型修改完成后会形成新的正式版本。出现版本冲突、目标不一致或预览失败时，不要强制覆盖，重新读取当前原型后再处理。

## 修改项目内原型

1. 进入项目并选择目标原型；
2. 点击「让 AI 修改」创建项目任务；
3. 确认负责人和需求；
4. 把平台生成的任务提示词交给 AI；
5. AI 提交修订后，负责人比较“正式版”和“待审修订”；
6. 负责人可以退回修改，也可以采用为新的正式版本。

项目任务不要求先签出。提交修订也不等于正式版本已经更新；只有负责人采用后才会更新正式版。

## 修改需求怎么写更有效

尽量包含三类信息：

- **修改范围**：具体页面、区域或交互；
- **保留范围**：哪些内容不要动；
- **验收结果**：修改后你要看到的可观察结果。

例如：“把客户列表的筛选区改成两行布局，保留现有字段和表格列；在 1280px 宽度下筛选项不换行溢出。”`
  },
  {
    slug: 'prototype-delivery',
    title: '构建、校验、发布与分享',
    summary: '理解 AI 什么时候才算真正交付完成，以及预览、正式版本和分享有什么区别。',
    version: '1.2',
    sortOrder: 45,
    categorySlugs: ['advanced-ai-quality', 'advanced-ai', 'advanced-platform-safety', 'beginner-platform-publish'],
    contentMarkdown: `## 先记住：AI 说“完成”不等于平台已经完成

一次原型交付至少要经过：

\`\`\`text
本地实现 → 构建与校验 → 上传伏羲 → 平台回读 → 浏览器验收
\`\`\`

普通用户不需要手工执行这些技术步骤，但应该用平台结果判断是否真的完成。

## 交付完成要检查什么

至少确认：

- 目标原型正确；
- 版本已经按预期变化；
- 预览可以打开；
- 设计说明和入口文件正常；
- 关键页面和操作可以真实使用。

“构建通过”“ZIP 已上传”或“接口返回 200”都只是中间状态。

## 预览、正式版本、项目修订

- **预览**：你现在看到的页面是否能正常运行；
- **正式版本**：当前正式采用的版本；
- **项目待审修订**：AI 为项目任务提交的修改，负责人采用前不会影响正式版。

因此，在项目里看到修订已经提交，不代表正式版本已经改变。

## 分享给别人

从原型详情生成完整分享链接，把这个链接直接发给评审者即可。分享只提供被分享内容的预览能力，不授予平台管理权限。

如果对方打不开，请让对方从**完整分享链接**重新打开，并使用同一个正常浏览器完成跳转；不要只复制浏览器跳转后的预览地址。

## 给 AI/排障人员的执行边界

在需要排查交付问题时，AI 会按 build、项目校验、打包、ZIP 校验、交付和 readback 的顺序执行。出现部分失败或版本冲突时，应先读取平台当前状态，不要盲目重复上传。`
  },
  {
    slug: 'faq',
    title: '常见问题',
    summary: '快速回答接入、修改、项目审核、预览、分享和权限中的常见疑问。',
    version: '1.2',
    sortOrder: 50,
    categorySlugs: ['beginner-platform', 'advanced-platform', 'advanced-platform-safety'],
    contentMarkdown: `## 应该选择哪个 AI Host？

优先使用 WorkBuddy；Cursor 也支持。当前 Codex 不提供确定性自动安装。如果页面没有把某个 Host 列为可用选项，不要自己猜配置路径。

## 为什么接入后还要重新加载或新开会话？

AI 工具通常会在会话启动时加载 MCP 和 Skill。接入完成后，旧会话可能还在使用旧状态，所以需要重新加载或打开新会话再确认。

## AI 已经说“上传成功”，为什么还不能算完成？

因为上传只是交付的一部分。你还需要看到伏羲中的目标原型、正确版本和可用预览。以平台回读和真实页面为准。

## 项目里的修订已经提交，为什么正式版没变？

这是正常的。项目修改先形成“待审修订”，负责人采用后才会成为正式版本。负责人也可以退回，AI 修改后再次提交。

## 项目任务需要先签出吗？

不需要。当前项目修改主流程使用 Task v2。签出/签入只保留给兼容或存量场景，不是普通项目任务的前置步骤。

## 为什么复制浏览器跳转后的预览地址会打不开？

请分享平台生成的**完整分享链接**，不要只复制跳转后的地址。完整分享链接会建立这次受控预览所需的访问状态。

## 我没有权限怎么办？

平台角色、项目角色和审核权限并不相同。联系项目负责人或平台管理员申请当前动作所需权限，不借用账号、token，也不要用删除、回滚等高风险操作绕过权限。

## 原型预览打不开怎么办？

先保留当前版本和 AI 返回的错误，然后查看《故障排查与安全边界》。不要先删除版本再重做。`
  },
  {
    slug: 'project-collaboration',
    title: '项目协作：Task v2 与候选审核',
    summary: '用“任务 → 待审修订 → 负责人审核 → 正式版本”完成项目内多人协作。',
    version: '1.2',
    sortOrder: 55,
    categorySlugs: ['advanced-platform', 'advanced-platform-project', 'advanced', 'advanced-platform-safety'],
    contentMarkdown: `## 项目协作的核心模型

项目里的修改不会让 AI 直接覆盖正式版本，而是走一条可审核的流程：

\`\`\`text
发起任务 → 负责人接受/推进 → AI 提交待审修订
→ 负责人退回或采用 → 采用后形成新的正式版本
\`\`\`

这样可以让多人协作时始终保留一个明确的正式版本。

## 负责人先准备项目

项目 Owner/Admin 负责：

1. 创建项目并维护菜单；
2. 把已有原型绑定到项目节点；
3. 添加项目成员并设置角色；
4. 为需要修改的原型创建任务、指定负责人；
5. 审核待审修订并决定退回或采用。

## 编辑者怎么参与

1. 打开分配给自己的项目任务；
2. 接受任务并确认需求；
3. 把平台生成的任务提示词交给 AI；
4. AI 完成后提交待审修订；
5. 等待负责人审核；被退回时按退回意见继续修改。

当前 Task v2 不要求先 checkout/checkin。

## 负责人怎么审核

任务页会突出展示：

- **当前正式版**：项目现在正在使用的版本；
- **待审修订**：AI 本次提交、尚未采用的修改；
- **历史尝试**：之前被替换或退回的提交记录。

负责人可以：

- **退回**：写明需要继续修改的地方；
- **采用为正式版**：确认修订后生成新的正式版本。

同一个任务任一时刻只保留一个当前待审修订，新的提交会把之前的尝试留在历史中。

## 角色边界

- **Owner/Admin**：管理项目、成员、任务和采用动作；
- **编辑者**：接受任务并提交修订；
- **查看者**：查看项目和预览，不参与修改。

快照、回滚、强制释放编辑锁等属于高风险或兼容能力，不是普通 Task v2 的步骤。`
  },
  {
    slug: 'spec-and-quality',
    title: '规范、运行时与输出模式',
    summary: '需要控制组件规范或实现方式时再看这篇；普通原型可以直接使用平台默认值。',
    version: '1.2',
    sortOrder: 65,
    categorySlugs: ['advanced-ai-quality', 'advanced', 'advanced-ai', 'advanced-ai-profile'],
    contentMarkdown: `> 如果你只是使用伏羲生成普通原型，不需要先理解这一页。使用平台默认值即可。

## 三个概念分别解决什么问题

- **prototype spec**：规定原型应该遵循哪套设计和组件规范；
- **runtime profile**：规定实际使用哪套前端运行时和组件实现；
- **输出模式**：规定这次更偏“快速验证”还是“严格证明组件实现”。

一次任务只使用一套明确的组合，避免不同规范和组件库混用。

## 什么时候需要你选择

- 有明确的公司/项目组件规范时；
- 需要证明原型真实使用指定组件时；
- 修改已有原型，必须保持原技术栈时；
- AI 无法判断原有实现，需要你确认时。

没有这些要求时，优先使用平台默认值。

## 两种输出模式

- **alignment**：适合快速验证需求、布局、密度、状态和关键交互；
- **implementation-proof**：适合需要严格证明所选组件规范被真实使用的场景。

如果指定组件没有对应能力，AI 应明确说明差距，而不是做一个“看起来像”的替代品冒充。

## 修改已有原型

已有原型应优先保持原有技术栈和组件规范。如果 AI 无法从现有代码、依赖或设计说明判断，应停止并向你确认，不要自行切换组件库。

## 最终仍以页面结果为准

无论选择哪种模式，都需要完成构建、平台交付和真实页面验收。静态检查通过不能替代浏览器中的实际效果。`
  },
  {
    slug: 'prompt-recipes',
    title: '如何向 AI 描述原型需求',
    summary: '不用学习底层工具调用；把目标、范围、约束和验收结果说清楚即可。',
    version: '1.2',
    sortOrder: 75,
    categorySlugs: ['advanced-ai-quality', 'beginner-ai', 'advanced-ai', 'advanced-ai-profile'],
    contentMarkdown: `## 先记住一个原则

你不需要告诉 AI 应该调用哪些伏羲工具，也不需要自己填写任务 ID、候选 ID 或内部执行参数。优先从伏羲页面发起任务，让平台生成完整提示词。

你真正需要补充的是：**要解决什么问题、改什么、不改什么、怎样算完成。**

## 创建新原型

可以按这个结构描述：

\`\`\`text
原型名称：
目标用户：
用户要完成的核心任务：
必须包含的页面：
关键操作：
需要覆盖的状态：
不在本次范围内：
验收结果：
参考文件/需求文档：
\`\`\`

例如：

\`\`\`text
创建一个“客户管理”原型。
目标用户：销售人员。
核心任务：筛选客户、查看客户详情、记录跟进结果。
必须包含：客户列表、客户详情。
关键操作：按跟进状态筛选；新增跟进记录。
不做：客户导入和权限管理。
验收：从列表筛选“待跟进”客户，打开详情并新增一条跟进记录后能看到最新记录。
需求文件：<完整本地路径>
\`\`\`

## 修改独立原型

重点写三件事：

\`\`\`text
要改：
必须保留：
验收结果：
\`\`\`

例如：

\`\`\`text
要改：把客户列表筛选区从一行改成两行，并压缩顶部留白。
必须保留：现有筛选字段、表格列和详情跳转。
验收：1280px 宽度下筛选项不溢出，点击“查询”和“重置”行为保持不变。
\`\`\`

## 修改项目里的原型

从项目目标节点点击「让 AI 修改」后，只需要补充业务需求和验收标准。平台会带上项目、任务、原型和版本上下文。

例如：

\`\`\`text
修改目标：订单详情页增加“异常原因”展示。
必须保留：现有订单状态和操作按钮。
验收：异常订单显示异常原因；正常订单不出现该区域。
\`\`\`

提交后等待负责人审核，不需要在提示词里手工写“采用”“候选 ID”等内部流程。

## 什么样的要求最容易返工

- “做得高级一点”
- “优化一下交互”
- “参考常见后台”
- “你看着办”

这些描述缺少可观察结果。尽量把主观要求改成具体场景、约束和验收标准。`
  },
  {
    slug: 'troubleshooting',
    title: '故障排查与安全边界',
    summary: '按“现象 → 先做什么 → 不要做什么”排查接入、交付、项目任务和分享问题。',
    version: '1.2',
    sortOrder: 85,
    categorySlugs: ['advanced-platform', 'advanced-ai-quality', 'beginner-platform-mcp', 'advanced-platform-safety', 'beginner-platform-publish'],
    contentMarkdown: `## 接入后 AI 看不到伏羲

先做：

1. 确认接入时选对了当前 AI Host；
2. 重新加载客户端或打开新会话；
3. 让 AI 再次验证伏羲连接。

如果仍失败，重新从伏羲生成一次接入提示词，并保留 AI 返回的失败阶段。

不要做：

- 不要扫描多个目录猜配置；
- 不要自己拼装替代安装命令；
- 不要把“文件已经存在”当成“客户端已经加载”。

## 原型构建或上传失败

先保留 AI 返回的完整错误。让 AI 根据失败阶段修复后重新构建和校验。

不要因为一次失败就删除正式版本，也不要连续重复上传碰运气。

## AI 说完成，但伏羲里看不到正确结果

以伏羲为准，检查：

- 是否是正确的目标原型；
- 版本是否变化；
- 预览是否可用；
- 设计说明是否与本次修改一致。

如果 AI 报告“部分交付失败”或版本冲突，让它先读取平台当前状态，再决定是否重试。

## 项目任务提交后正式版没变

这是正常的。AI 提交的是待审修订，负责人采用后才成为正式版本。

如果任务提示你走旧 change/handoff 流程，或 AI 要求先 checkout，请回到项目页面重新生成当前任务提示词。

## 分享链接打不开

让访问者从平台生成的**完整分享链接**重新打开，并在同一个正常浏览器中完成跳转。不要只复制浏览器跳转后的预览地址。

## 权限不足

查看、编辑、提交修订、采用、发布、分享、删除、回滚不是同一种权限。联系项目负责人或平台管理员申请当前动作需要的权限。

## 哪些操作要特别谨慎

删除原型、回滚版本、恢复项目快照、强制释放编辑锁都会改变已有状态。普通创建和修改任务不应该自动执行这些操作。

遇到不确定情况时，优先保留当前版本、任务和错误信息，再继续排查。`
  }
];

module.exports = { HELP_DOCUMENT_DEFAULTS };
